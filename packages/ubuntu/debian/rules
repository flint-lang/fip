#!/usr/bin/make -f

%:
	dh $@

# No compilation, we ship a prebuilt static binary
override_dh_auto_configure:
override_dh_auto_build:
override_dh_auto_test:

# Make sure the binary stays executable
override_dh_fixperms:
	dh_fixperms
	chmod 755 debian/fip-c/usr/bin/fip-c
